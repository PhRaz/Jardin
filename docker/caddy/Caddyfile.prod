# APP_DOMAIN=:80          → HTTP sur IP (test sans domaine)
# APP_DOMAIN=monsite.fr   → HTTPS automatique via Let's Encrypt

{env.APP_DOMAIN} {
	root * /var/www/html/public

	encode gzip

	@notStatic {
		not file
	}
	rewrite @notStatic /index.php

	php_fastcgi php:9000

	file_server
}
